# Local development — points at your local backend
VITE_API_URL=http://localhost:5000/api

# Production (set this in Vercel's Project Settings → Environment
# Variables, NOT in a committed file) — points at your deployed
# Render backend:
# VITE_API_URL=https://your-backend.onrender.com/api
